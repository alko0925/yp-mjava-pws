spring:
  application:
    name: yp-mjava-pws

  sql:
    init:
      mode: always

  datasource:
    url: jdbc:h2:mem:usersdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

post:
  shorttext:
    size: 128

server:
  port: 8085